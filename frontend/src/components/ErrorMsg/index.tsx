// Styles
import './ErrorMsg.scss'

const ErrorMsg = () => (
  <div className='ErrorMsg'>
    <div>Error while trying to access the server.</div>
    <div>Please make sure the backend is running and try again.</div>
  </div>
)

export default ErrorMsg
